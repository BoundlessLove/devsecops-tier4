# k8s/deployment.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: aks-demo-deployment
  labels:
    app: aks-demo
spec:
  replicas: 2
  selector:
    matchLabels:
      app: aks-demo
  template:
    metadata:
      labels:
        app: aks-demo
    spec:
      containers:
        - name: aks-demo
          image: ${ACR_NAME}.azurecr.io/${IMAGE_NAME}:${IMAGE_TAG}
          ports:
            - containerPort: 8080
