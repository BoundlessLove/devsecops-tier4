apiVersion: v1
kind: Service
metadata:
  name: aks-demo-service
spec:
  type: ClusterIP
  selector:
    app: aks-demo
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
