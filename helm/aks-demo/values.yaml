acrName: devsecopstier2acr
imageName: aks-demo
imageTag: latest

replicaCount: 2

service:
  port: 80
  targetPort: 8080

ingress:
  enabled: true
  className: nginx
  host: aks-demo.example.com
  tls:
    enabled: false
    secretName: ""
